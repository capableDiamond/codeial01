let password=document.getElementById("password"),confirmPassword=document.getElementById("confirm-password"),submitButton=document.getElementById("reset-password-button");function checkIfSame(){password.value!=confirmPassword.value?submitButton.disabled=!0:submitButton.disabled=!1}password.addEventListener("change",checkIfSame),confirmPassword.addEventListener("change",checkIfSame);